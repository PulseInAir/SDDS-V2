compare [2.png](file;file:///d%3A/Websites/SDDS-V2/2.png) with [1.png](file;file:///d%3A/Websites/SDDS-V2/1.png) Did you noticed any difference ? [1.png](file;file:///d%3A/Websites/SDDS-V2/1.png) has a smooth overall design. Whereas, [2.png](file;file:///d%3A/Websites/SDDS-V2/2.png) has two contrasting design. I want you to intelligently convert the design of [2.png](file;file:///d%3A/Websites/SDDS-V2/2.png) to a smooth design as like [1.png](file;file:///d%3A/Websites/SDDS-V2/1.png) . The whole design should be covered by guided journey only. There should not be any header menus like guided journey, identity profile, Assessment years etc. They should be hidden under each step.

In step 1, "New Client" will be added from the pool of all client list. This window should show all the details contained in the following header menu, "Identity Profile", "Assessment Year" & "Credentials" under this one window. This will be new incarnation of the client for a new AY. Before that, the client is dormant. One can edit the new client data here. This step should contain a "Create Case" button to start the filling process. When assessment year is selected, the system should calculate itself, if that AY case was already created or not. If created then instead of create case button, show the open case button. It will lead to all filing details window for that particular client. If not created, the create case button should appear and gets activated. After clicking on Create Case button, a new window should open as step 2. Step 1 will subsume two header menu, "Identity Profile", "Assessment Year" & "Credentials".

Step 2, "Client Status". In step 2, the user has to select the status of the client. This window should show all the details contained in the header menu, "Fillings". If the user selects filling queue status and saves it, do nothing. Just show it like that. But if the user selects Filed status and save it, then automatically the window should change and shift to step 3. Step 2 will subsume one header menu, "Fillings".

Step 3, "Invoice". This window should show all the details contained in the following header menu, "Documents", "Invoices & Payments" & "Refunds". This step includes two windows. At first window, the upload file windows should open where user should be facilitated with upload file container. After attaching the file & clicking on the upload button, the second window should open within the same step. This window should show all the forms of different subsumed header menus. In this window, the charges should auto populate following two parameters set in the setting, one, ITR No. & second, Refund Amount. There should be one option to reupload the ITR V here. Also, options to edit the invoice details. It should contain buttons to create Draft Invoice or Final Invoice. But clicking both will forward the step to the next one, i.e. Payment. Step 3 will subsume the following header menus, "Documents", "Invoices & Payments" & "Refunds".

Step 4, "Payment" step. In this step, if user clicks on Draft Invoice in step 3, show the edit invoice form in step 4 also. After finalising the draft invoice in step 4, the final invoice generates. Then, ask for Payment details. Consider this as 4 stages of the process. Stage 1, Draft Invoice editing and finalisation, then save. Stage 2, Final Invoice complete dialogue box with the Final Invoice PDF downlaod, printing & save option. Stage 3, Payment Form. Complete all stages and move on to next step. Use, stage by stage revealing method. Don't reveal 2nd stage before completing the 1st one. On the other hand, if user generates final invoice in step 3, then just show the paymnent details form. Filling and saving this form will lead to the next step, i.e. step 5. If no payment details were entered within due dates, then system informs the user, using Dashboard tiles, to act on dues. Step 4 will subsume the following header menu, "Communication & Activity".

Step 5, "Next AY Preparation" step. In this step, the window shows all the previous filing details along with few to dos, like, Counting timer for the next filling session which starts from June every year. Then, option to call the client for the new session. and a button to Create a new case. This button will again reverse itself to the first step window, i.e. "New Client".

Each step explained above should be one indivisual header menu of itself replacing the current one. They should subsume the current workings of the existing header menus and work as interlinked header menus. Each button click should automatically transfer the user to the next header menu magically. Each menu is linked with one another. One click will land the user to the next menu. Before writing the codes to build, show me the new "header menu" block.